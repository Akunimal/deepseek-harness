diff --git a/vendor/deepseek-harness/packages/mcp/mcp-client/src/transport.ts b/vendor/deepseek-harness/packages/mcp/mcp-client/src/transport.ts
index bc7fd1cc08..9591a82e99 100644
--- a/vendor/deepseek-harness/packages/mcp/mcp-client/src/transport.ts
+++ b/vendor/deepseek-harness/packages/mcp/mcp-client/src/transport.ts
@@ -12,6 +12,9 @@ import { StreamableHTTPClientTransport } from '@modelcontextprotocol/sdk/client/
 import { scrubbedParentEnv } from '@deepseek-ai/dsh-subprocess'
 import type { Config } from './index.ts'
 
+/** A transport may expose a diagnostic stderr stream for its child process. */
+export type McpTransport = Transport & { stderr?: NodeJS.ReadableStream | null }
+
 /**
  * The subprocess seam's scrubbed parent env (credential-shaped and stale
  * `DSH_*` names dropped), plus the spec's explicit env. The MCP SDK owns the
@@ -28,7 +31,7 @@ function buildChildEnv(extra: Record<string, string>): Record<string, string> {
  * @param config - Resolved plugin config discriminated on `transport`.
  * @returns A connected-ready MCP Transport (stdio or Streamable HTTP).
  */
-export function createTransport(config: Config): Transport {
+export function createTransport(config: Config): McpTransport {
   switch (config.transport) {
     case 'stdio':
       return new StdioClientTransport({
@@ -36,7 +39,11 @@ export function createTransport(config: Config): Transport {
         args: config.args,
         env: buildChildEnv(config.env),
         cwd: config.cwd,
-      })
+        // Do not inherit MCP stderr into Electron. It is not a tool protocol
+        // stream, can surface as a transient console on Windows, and otherwise
+        // makes startup failures invisible to the application logger.
+        stderr: 'pipe',
+      }) as McpTransport
     case 'streamable-http':
       // The MCP SDK's StreamableHTTPClientTransport has optional callback
       // properties typed without `| undefined` (exactOptionalPropertyTypes
@@ -45,6 +52,6 @@ export function createTransport(config: Config): Transport {
       return new StreamableHTTPClientTransport(
         new URL(config.url),
         { requestInit: { headers: config.headers } },
-      ) as Transport
+      ) as McpTransport
   }
 }
